#include <stdio.h>

void main() {

	int count = 0;

	while (count < 10)
	{
		printf("%d\n", count);
		count++; //Count is a counter
		//printf("%d\n", count); PRINT FROM 1 TO 10 BC IT ADDS TO COUNT BEFORE IT PRINTS
	}
}
